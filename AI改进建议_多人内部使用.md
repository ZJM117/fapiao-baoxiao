# 发票报销项目：多人内部使用改进建议

## 文档目的
本文件用于指导 AI 或开发人员把当前单人/低并发工具升级为公司内部多人使用的稳定系统。只允许在确认兼容现有数据后实施，禁止覆盖或删除现有 Excel、票据、输出和备份。

## 当前判断
项目功能完整，适合个人或低并发使用。多人场景的核心风险是 Excel 并发写入导致数据覆盖、共享口令无法追责、权限边界不足、文件接口暴露过多、耗时任务缺少统一任务状态，以及测试尚未形成稳定自动化体系。

## P0：必须先改

### 1. 用 SQLite 事务层替代在线 Excel 写入
问题：`ledger.py` 采用“读整个 Excel、改内存、整表写回”；多人同时操作时，后保存者可能覆盖先保存者的修改。

建议：
1. 新增 SQLite 数据库，建立 `invoices`、`users`、`audit_logs`、`jobs`、`generated_files` 等表。
2. 新增、修改、删除、状态变更全部使用事务。
3. 发票号码和无号码凭证指纹建立唯一索引。
4. Excel 改为导入、导出和人工查看格式，不再作为并发写入源。
5. 提供旧 Excel 到 SQLite 的迁移命令，迁移前自动备份原文件。

验收：两个用户同时改不同记录不会互相覆盖；同时操作同一条记录时只有一个成功并收到明确提示；重启后数据完整。

### 2. 增加用户、角色和权限
问题：共享口令无法识别操作者，也无法限制不同人员的功能和数据范围。

角色建议：管理员、财务审核员、业务人员、只读用户。

建议：用户名唯一；密码使用 Argon2id 或 bcrypt 哈希；服务端对每个写接口再次校验权限；按部门、项目或报销人限制可见范围；支持禁用用户、重置密码和强制退出会话。

验收：普通用户不能调用管理员接口；用户不能查看未授权部门的台账、原件和输出文件；每次修改都能关联到具体用户。

### 3. 建立审计日志
问题：现有日志偏排障，不能完整回答谁在何时改了哪张票以及改前改后内容。

建议：登录成功/失败、上传、导入、修改字段、状态变更、删除、恢复、导出、生成报销单、配置修改全部写入审计表。字段至少包括时间、用户、IP、操作类型、对象 ID、修改前 JSON、修改后 JSON、结果和失败原因。增加管理员查询页面。

### 4. 增加后台任务和并发控制
问题：解析、生成 PDF、合并 PDF 等耗时操作可能阻塞请求，重复点击还可能创建重复任务。

建议：操作先创建任务 ID；任务状态包括排队、运行中、成功、失败、取消；相同数据集的导入/重新识别/批量更新互斥；服务重启后未完成任务恢复为失败或可重试。

## P1：强烈建议改

### 5. 引入明确的报销状态流转
建议状态：`待识别 → 待补充 → 待提交 → 待审核 → 已退回 → 审核通过 → 已报销 → 已作废`。状态变化必须记录操作者、时间和备注。审核通过后普通用户不能直接改金额、号码、日期等关键字段。

### 6. 报销单使用草稿、提交、锁定和版本快照
生成报销单前先保存草稿；提交后锁定发票集合和金额快照；审核和最终 PDF 使用快照；台账变化后提示单据过期并要求重新生成；显示版本号和生成时间。

### 7. 收紧文件访问范围
当前 `/files/data/` 可能暴露配置、缓存、日志、手机号映射和备份。建议只开放有权限的票据原件和输出文件；禁止浏览数据库、配置、日志、缓存和备份；下载前做对象权限校验；不要向浏览器暴露服务器绝对路径。

### 8. 限制上传和 ZIP 解压
限制扩展名、单文件大小、文件数量、总空间和请求超时。ZIP 必须逐个安全解压，拒绝绝对路径、`..`、符号链接，并限制成员数量、解压总大小和压缩比，防止磁盘耗尽。

### 9. 增加组织维度和筛选
新增部门、项目、成本中心、报销人、审核人、归属月份和批次。默认按权限显示数据；支持保存常用筛选条件和导出当前筛选结果。

### 10. 改进导入和人工处理
导入前提供预览：新增、重复、无法识别、疑似异常数量；确认后正式入库。解析失败进入待人工处理清单；显示缺失字段和识别置信度；支持批量补充和断点续跑。

### 11. 加强重复报销规则
除发票号码和指纹外，增加号码/日期/金额组合校验；同一报销人、项目、相近日期和相同金额提示疑似重复；关联红字与蓝字发票；人工确认非重复时要求填写原因。

## P2：提升好用程度

1. 首页增加待办工作台：待补充、待核对、待审核、解析失败、疑似重复、失败任务。
2. 增加批量分配报销人、部门、项目、费用类别、审核人和批次。
3. 增加站内通知中心；后续可接企业微信、钉钉或邮件，通知失败不能影响主流程。
4. 增加管理员备份、恢复、恢复前预览和恢复后自动备份。
5. 增加健康检查：数据库、磁盘、票据目录、PDF 引擎、备份时间、失败任务和版本号。

## 部署和安全

1. Docker 服务器模式禁止使用默认密码 `change-me`。
2. 通过 HTTPS 或反向代理访问；HTTPS 下 Cookie 增加 `Secure`。
3. 增加登录失败限流、会话过期和管理员强制退出。
4. 生产环境不要直接暴露开发 HTTP 服务到公网。
5. 固定 Python 和第三方库版本，使用依赖锁定文件构建镜像。
6. 容器使用非 root 用户运行。
7. 浏览器只收到用户可理解的错误；详细异常只写服务端日志。
8. 定期备份数据库、原件和输出，并做恢复演练。

## 测试要求

建立统一 `pytest` 入口，至少覆盖：并发修改、并发导入、重复发票、红字发票、无号码凭证、权限越权、路径穿越、恶意 ZIP、超大上传、任务重试、旧 Excel 迁移、备份恢复、服务重启和报销单锁定流程。

当前 `测试/diag_ui_render.py` 第 72-73 行存在 f-string 语法错误，应先修复或移除该诊断脚本，再迁移为自动化测试。

## 推荐实施顺序

1. SQLite 数据层和 Excel 迁移工具。
2. 用户、角色、权限和审计日志。
3. 并发控制、后台任务和任务状态。
4. 文件权限、上传限制和 ZIP 安全处理。
5. 报销单草稿、审核、锁定和版本快照。
6. 待办工作台、批量操作、通知、备份恢复和健康检查。
7. 完善自动化测试和恢复演练。

## AI 执行要求

每次改造前先检查现有代码和数据格式；先写迁移和兼容方案，再做最小实现；每阶段增加回归测试；使用真实数据副本演练；输出修改文件、风险、测试命令和未完成事项。禁止直接删除或覆盖用户数据。

## 完成标准

多人同时操作不丢数据；每个用户有明确身份和权限；关键修改可追溯；报销单审核和锁定可追溯；原件和敏感配置不会越权下载；失败任务可查看、重试和恢复；备份恢复流程可验证。
