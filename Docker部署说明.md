# 部署到 NAS（Docker Compose）

一句话：把程序跑成一个容器，**浏览器访问 `http://NAS的IP:8766` 就是界面**，台账和生成的报销单都落在 NAS 的共享文件夹里，跟在本机双击 bat 用的完全是同一套代码。

> 你的机器：绿联 DXP4800 Plus（x86_64），下面按绿联的 Docker 应用（UGOS Pro）写的。
> 群晖/威联通只是「新建项目」的入口位置不同，compose 内容一模一样。

> 📌 **想省掉「在 NAS 上构建」这一步**（容器几十秒起好，而不是等 3~5 分钟）：
> 看另一份 **`GitHub与镜像部署说明.md`** —— 代码推到 GitHub，GitHub 自动把镜像构建好，
> NAS 只负责 `docker compose pull`。本文是「NAS 上自己构建」的完整流程，两条路都行。

---

## 一、准备工作

1. NAS 上装好 **Docker** 应用（绿联的「Docker」，老版本叫「容器管理」）。
2. 有一个能放东西的共享文件夹，比如 `docker`。建议把项目放在：
   ```
   /volume1/docker/fapiao/
   ```
   （`volume1` 还是 `volume2` 看你自己的存储池，在「文件管理」里能看到实际路径）

---

## 二、把项目文件夹放到 NAS

把本机这个文件夹整个拷到 NAS 上：

```
发票报销/            ← 整个文件夹拷过去，改名叫 fapiao
├── app_web.py            ← 界面主程序（含登录、权限、接口）
├── app_paths.py          ← 路径常量 + FB_* 环境变量
├── db.py                 ← 台账数据库(SQLite) + 用户/权限/审计
├── ledger.py             ← 台账读写、导入导出
├── invparse.py           ← 票据解析
├── report_pdf.py         ← 报销单 / 合并
├── attachment.py         ← 发票与行程单配对
├── traveler.py           ← 出行人识别
├── gui/                  ← 界面文件
├── Dockerfile            ← 容器定义
├── docker-compose.yml    ← 一键部署
└── .dockerignore         ← 排除本机运行数据，别打进镜像
```

`缓存/`、`输出/`、`台账备份/`、`测试/` 这些不用拷（`.dockerignore` 已经把本机的运行数据排在镜像外了）。
**真实台账 `发票台账.xlsx`（给人看的快照）和 `发票台账.db`（台账真身）都别拷进去** ——
它们是数据，不是程序。想带着历史台账过去的话，见第五节末尾。

---

## 三、改两处配置

打开 `docker-compose.yml`：

```yaml
    environment:
      FB_PASSWORD: "change-me"     # ← ① 改成你自己的口令（管理员口令）
    volumes:
      - ./data:/data               # 台账/配置/输出 都在这（不用改）
      - ./票据:/票据               # ② 把你的票据文件夹挂到这里
```

- **口令**：`FB_PASSWORD`。它一次干三件事：**第一次启动自动创建管理员账号时用的初始密码**、
  **管理员自己的备用登录方式**、以及没账号时的门槛。留空 = 谁打开都不用登录 ——
  只在完全可信的内网才这么干。第一次启动后登录页上是这样：
  - 用户名 `admin` + 这个口令 → 管理员登录（推荐，审计里记的就是 `admin`）
  - 只填口令、用户名留空 → 也能进，身份记为「口令用户」，一样是管理员权限
  - 进去之后在「**设置 → 账号与权限**」给同事开各自的账号，他们就不必知道这个口令了
- **票据文件夹**：建议就用项目目录下的 `票据/` 子文件夹（第一次启动会自动建）。
  如果票据已经在 NAS 别的地方（比如 `/volume1/发票/报销`），把它挂进来更省事：
  ```yaml
      - /volume1/发票/报销:/票据
  ```
- **端口**：`8766` 被占用了就改冒号左边那个（比如 `18766:8766`）。

---

## 四、起服务

### 用绿联的图形界面

1. 打开 **Docker** → **项目** → **新建项目**
2. 项目名随便填（`fapiao`），路径选到刚才那个 `fapiao` 文件夹
3. 它会自动读 `docker-compose.yml`，直接点**部署 / 启动**
4. 第一次要构建镜像，**等几分钟**（要下 chromium 和中文字体）

### 或者用命令行（SSH 登进 NAS）

```bash
cd /volume1/docker/fapiao
docker compose up -d
docker compose logs -f          # 看到「[就绪] 请用浏览器访问」就是好了
```

---

## 五、打开界面

浏览器访问：

```
http://192.168.x.x:8766
```

（NAS 的 IP + 8766。**第一次登录**：用户名 `admin`，密码就是你设的那个 `FB_PASSWORD`）

进去以后：

1. **凭证入库**页 → 告诉程序票据在哪儿，两种方式随便挑：

   **① 传本机的文件夹上去（推荐）** —— 点「**上传本机文件夹…**」，或者直接把文件夹从资源管理器拖进页面。
   浏览器弹出的目录框是**你这台电脑**的，选好之后自动传到 NAS 上，落在
   `票据/上传/<你选的那个文件夹名>/…`，里面的子目录原样保留（发票和它的行程单必须还待在同一个文件夹里，
   配对才认得出来）。传完路径框会自动切到那个目录，直接点「开始入库」就行。
   同一个文件夹重复传不会重复写（同名同大小的直接跳过），传了什么、跳过什么都会写在日志里。

   **② 用 NAS 上已有的目录** —— 路径框里手填**容器内路径**，比如 `/票据`
   （是你 NAS 上 `fapiao/票据/` 那个目录，路径要按容器里的写法，不是你 NAS 上的绝对路径）。
   ⚠️ 「选择文件夹…」这个按钮在容器里点不了：容器里没有图形界面，而且它选的是 **NAS** 上的目录，
   不是你这台电脑的 —— 要动本机文件夹请用上面第 ① 种。

2. 点「开始入库」→ 解析 → 建台账
3. **凭证台账**页 → 查询、勾选、点「出行人核对」把出行人过一遍
4. **报销单**页 → 选单据类型（费用报销单 / 差旅费报销单 / 模板二）→ 生成 PDF + Excel
5. 生成的单据在「输出」列表里点**打开**就是浏览器里直接看 PDF，点**定位**是下载
6. **设置**页 →「**账号与权限**」→ 给同事开账号（每人选一个角色）。开完把地址和账号发给他们，
   各自登录即可；谁做了什么在「设置 → 台账数据库 → 审计日志」里都能查到

> **想把本机那份台账带过去**，两种任选：
> - **推荐**：把本机 `data/` 下的 `发票台账.db` 拷到 NAS 的 `fapiao/data/`（如果有
>   `发票台账.db-wal` / `-shm` 也一起拷），重启容器。这是台账真身，带过去跟本机一模一样。
> - 或者：把 `发票台账.xlsx` 拷到 `fapiao/data/`，重启容器 —— 程序发现库里还是空的，
>   会自动从这份 Excel 迁移进来。
>
> 无论哪种，**动手前先把 `fapiao/data/` 整个目录复制一份留底**。

---

## 六、数据都在哪

| 位置（NAS 上） | 内容 |
|---|---|
| `fapiao/data/发票台账.db` | **台账真身**（SQLite；账号、权限、审计记录也在这个库里）。备份就拷它 |
| `fapiao/data/发票台账.xlsx` | 台账的**导出快照**，双击用 Excel 打开看／打印。⚠️ 现在 Excel 不是真身了：你改了它系统不会自动变（程序重导前会先把你的改动另存成 `台账备份/发票台账_手改_<时间>.xlsx`）；想把改动并回系统，用「设置 → 台账数据库 → 从 Excel 导入」 |
| `fapiao/data/输出/` | 生成的所有报销单、合并 PDF、统计 Excel |
| `fapiao/data/输出/生成记录.json` | 「输出」列表里那些历史记录 |
| `fapiao/data/台账备份/` | 每次危险操作（删记录、清空、导入）前自动留的备份，`.db` 和 `.xlsx` 各一份 |
| `fapiao/data/缓存/` | 解析缓存、手机号对照表、出行人核对记录（可删，删了重新解析） |
| `fapiao/data/回收站/` | 删掉的生成文件挪到这里（删错能捞） |
| `fapiao/data/launch.log` | 运行日志，出问题先看它 |
| `fapiao/票据/上传/` | 从本机传上去的票据（相当于把本机那个文件夹复制了一份过来） |

> 「上传」里的文件是**副本**，会一直留着 —— 本机那份原件不受影响。传错了想清掉，
> 在 NAS 的文件管理器里删 `fapiao/票据/上传/` 下的东西就行（已登记进台账的记录不会跟着消失）。

**备份**就是直接拷 `fapiao/data/` 这个目录（或者 NAS 上给这个文件夹配个同步/快照任务）。
⚠️ 别在容器跑着的时候**只拷 `发票台账.db` 一个文件** —— SQLite 开了 WAL，有一部分数据可能还在
`发票台账.db-wal` 里，单独拷会得到一份不完整的。要么整目录拷，要么先 `docker compose stop` 再拷。

---

## 七、常见问题

**构建时卡住 / apt 下载失败**
`apt-get` 要连 Debian 源，国内有时慢。多等一会儿或重试；实在不行给 NAS 的 Docker 配个能用的 DNS / 代理。

**镜像挺大的（约 700MB）**
主要是 chromium（打印 PDF 用）+ 思源中文字体（不然报销单上的中文全是方块）。
这两个都不能省，省了 PDF 就出不来或打出方块。

**报销单 PDF 生成失败 / 提示没有产出文件**
看日志（`docker compose logs` 或 `data/launch.log`）。多半是 chromium 没装成 —— 重新构建一次。

**打印出来的 PDF 中文是方块**
说明镜像里缺中文字体。确认 Dockerfile 里 `fonts-noto-cjk` 没被删掉，然后 `docker compose up -d --build`。

**报销单上的时间和实际差 8 小时**
compose 里的 `TZ: "Asia/Shanghai"` 被改掉了，加回去重建。

**忘了口令**
改 `docker-compose.yml` 里的 `FB_PASSWORD`，然后 `docker compose up -d`（会重建容器，数据不动）。
⚠️ 这只影响「访问口令」这条路；各人账号的密码存在数据库里，不受它影响。
要是**管理员账号的密码**也忘了：登录时**用户名留空、只填访问口令**就能进去，再在设置页改回来。

**改了代码怎么生效**
`docker compose up -d --build`。数据在卷里，不受影响。

**升级到这一版（多用户版）之后，我原来的台账还在吗**
在。容器启动时会自动把 `data/发票台账.xlsx` 迁移进新建的数据库（`data/发票台账.db`），
**迁移前先备份原 Excel**。启动日志里会出现「台账数据库已就绪（N 行）」。
没迁上或者想核对行数：「设置 → 台账数据库」里能看到当前库里的条数，
「从 Excel 导入」可以再把某份 Excel 整表导进来。

**同一时间几个人一起用会不会冲突**
不会了。台账已经换成 SQLite 数据库（`data/发票台账.db`），写操作都走数据库事务：
两个人同时入库、同时改**不同**的行都不会互相覆盖；同一行被两个人同时改，只有先提交的那个成功，
另一个会提示「这行刚被改过，请重新加载」，不会静默丢数据。

**怎么给同事开账号 / 权限怎么分**
「设置 → 账号与权限」（只有管理员看得到）。新增账号时给每人选一个角色：

| 角色 | 看台账 | 入库 | 出报销单 | 改台账 | 删/清空 | 改设置 | 管账号 |
|---|---|---|---|---|---|---|---|
| 管理员 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 财务审核员 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ |
| 业务人员 | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| 只读用户 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

界面上藏起来的按钮只是「不显眼」，真正卡权限的地方在后端 —— 每个接口都会再查一次，
绕过界面直接发请求也越不了权。

**怎么知道谁改了什么 / 有人离职或密码泄露**
「设置 → 台账数据库 → 审计日志」：每条记录含时间、谁、来源 IP、动作、改前、改后，
登录失败和越权尝试也在里面。
要停谁的号：「设置 → 账号与权限」→ 那一行点**停用**或**强制退出**，
对方**已登录的会话会立刻失效**（不用重启容器）；忘了密码就点**改密码**。

**上传完的文件夹在哪、会占空间吗**
在 NAS 上 `fapiao/票据/上传/<你选的文件夹名>/`，跟传上去时一模一样的目录结构。
文件是**复制**过去的，本机原件不动；不用了在文件管理器里删掉即可（已入库的台账记录不会跟着消失）。

**上传中途失败 / 有的文件没传上去**
日志里会一个个列出来是哪个文件、为什么（同名同大小的会标「跳过」，那是正常的 —— 说明 NAS 上已经有这份了）。
已经传成功的不会重传，重新再传一次那个文件夹就行。单个文件上限 512MB。

**上传很慢**
几百张小票通常一两分钟。慢的话多半是 NAS 那块盘或者 Wi-Fi 的缘故 —— 传的时候界面会一直显示进度，
按文件一个个来（不并发），所以不会把 NAS 卡死。

---

## 八、本机（Windows）还能继续用吗

能，一模一样。双击 `启动发票报销工具.bat` 跑的就是同一套代码：
不设 `FB_*` 环境变量时，它自动退回「本机模式」—— 只监听 `127.0.0.1`、不要求登录、
5 分钟没操作自动退出、数据就放在程序目录里。两边互不影响。

只有一点不一样：本机模式**不弹登录**，身份记作「本机」、权限等同管理员，
审计日志里也会把每件事记在「本机」名下。本机的台账同样在 `发票台账.db` 里
（就在程序目录），跟 NAS 那份是**两个独立的库** —— 想合到一起，
在一侧导出一份 Excel，拷到另一侧的 `data/` 目录，用「设置 → 台账数据库 → 从 Excel 导入」。

想随时确认当前是哪一档：看 `launch.log` 第一行会写「运行模式：本机 / 服务器（Docker/NAS）」。

---

## 九、部署前先在电脑上自检一遍（不用装 Docker）

**镜像构建**这一步得等真在 NAS 上 `docker compose up -d` 才知道成不成（本机没装 Docker，
替代不了）。但**「第一次启动会发生什么」可以先在本机完整验证** —— 同一套代码、同一批环境变量：

```bat
cd /d E:\桌\个人文件同步\workbuddy\发票报销
python 测试\diag_deploy_first_run.py
```

它在 `%TEMP%` 里仿一份「镜像 + 空的 `/data` 卷 + 一份带数据的老台账 Excel」，起真实服务走真实 HTTP，
逐条验证：

- 镜像里没混进台账数据库 / Excel / 配置（`.dockerignore` 真的挡住了）
- 首启自动建库，老 Excel 的行被搬进库，**且迁移前自动备份了原 Excel**
- 首启自动建管理员账号 `admin`（初始密码就是 `FB_PASSWORD`）
- 三条登录路：`admin`+口令、**用户名留空只填口令**、错口令被挡
- `/api/ledger_db_info` 报的条数与库里实际一致、唯一索引建上了
- 再启动一次**不会重复迁移**（行数不会翻倍）、账号重启后仍有效

跑完打印「全部通过」就说明 NAS 上第一次启动不会出岔子。全程无窗口，真实数据一个字节不动。

⚠️ 这一步过了 ≠ 构建一定成功。`apt-get` 拉 chromium / 中文字体那一关要等 NAS 上真的构建才知道，
卡住的话看第七节「构建时卡住」那条。
